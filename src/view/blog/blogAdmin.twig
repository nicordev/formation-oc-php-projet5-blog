{% extends "layout.twig" %}

{% block title %}
    Administration
{% endblock %}

{% block header %}
    <h1>Gestion des articles</h1>
{% endblock %}

{% block content %}
    <section id="blog-admin">

        {# Posts#}
        <div class="admin-box w100">
            <h2>Articles</h2>

            <p>
                <a class="btn" href="?page=post-editor">Ajouter un article</a>
            </p>

            {% include "blog/message.twig" %}

            <table>
                <thead>
                    <tr>
                        <th>Titre</th>
                        <th>Auteur</th>
                        <th>Etiquettes</th>
                        <th>Commentaires</th>
                        <th>Date de création</th>
                    </tr>
                </thead>

                <tbody>
                {% for post in posts %}
                    <tr>
                        <td>
                            <form action="?page=post-editor" method="post">
                                <input type="hidden" name="post-id" value="{{ post.getId }}">
                                <input class="admin-blogpost-title" type="submit" value="{{ post.getTitle }}">
                            </form>
                        </td>
                        <td>
                            coming soon...
                        </td>
                        <td>
                            {% if post.getTags() %}
                                {% for tag in post.getTags() %}
                                    {{ tag.getName() }}
                                {% endfor %}
                            {% else %}
                                -
                            {% endif %}
                        </td>
                        <td>
                            coming soon...
                        </td>
                        <td>
                            {{ post.getCreationDate }}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>

                <tfoot>
                    <tr>
                        <th>Titre</th>
                        <th>Auteur</th>
                        <th>Etiquettes</th>
                        <th>Commentaires</th>
                        <th>Date de création</th>
                    </tr>
                </tfoot>
            </table>
        </div>

        {# Tags #}
        <div class="admin-box">
            <h2>Etiquettes</h2>

            <form id="admin-tag-list-form" action="?page=blog-admin" method="post">

                <input id="update-tag-list-btn" class="btn" type="submit" value="Mettre à jour la liste des étiquettes">

                <ul id="tag-list">
                    {% for tag in tags %}
                        <li>
                            <input type="hidden" name="tag_ids[]" value="{{ tag.getId() }}">
                            <input class="available-tag" type="text" name="tag_names[]" value="{{ tag.getName() }}" onkeyup="myApp.refreshValue(event); highlightIncorrectTags();"><span class="closing-cross" onclick="myApp.eraseTool.eraseElementFromEvent(event)">X</span>
                        </li>
                    {% endfor %}

                    <li id="new-tag-input-li">
                        <input id="new-tag-input" type="text" name="new-tag-input" placeholder="Ajouter une nouvelle étiquette">
                        <button type="button" id="new-tag-btn" class="btn btn-small">+</button>
                    </li>
                </ul>
            </form>
        </div>

        <div class="admin-box">
            <h2>Catégories</h2>

            <form id="admin-category-list-form" action="?page=blog-admin" method="post">

                <input id="update-category-list-btn" class="btn" type="submit" value="Mettre à jour les catégories">

                <ul id="category-list">
                    {% for category in categories %}
                        <li>
                            <input type="hidden" name="category_ids[]" value="{{ category.getId() }}">
                            <input class="available-category" type="text" name="category_names[]" value="{{ category.getName() }}" onkeyup="myApp.refreshValue(event); highlightIncorrectCategories();"><span class="closing-cross" onclick="myApp.eraseTool.eraseElementFromEvent(event)">X</span>
                        </li>
                    {% endfor %}

                    <li id="new-category-input-li">
                        <input id="new-category-input" type="text" name="new-category-input" placeholder="Ajouter une nouvelle catégorie">
                        <button type="button" id="new-category-btn" class="btn btn-small">+</button>
                    </li>
                </ul>
            </form>
        </div>
    </section>
{% endblock %}

{% block footer %}
    {% include "blog/adminFooter.twig" %}
{% endblock %}

{% block scripts %}
    {{ parent() }}
    <script src="public/js/blogAdmin.js"></script>
{% endblock %}