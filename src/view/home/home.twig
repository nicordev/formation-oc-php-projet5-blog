{% extends "layout.twig" %}

{% block title %}
    Accueil
{% endblock %}

{% block header %}
    <h1>Mon super site</h1>
{% endblock %}

{% block content %}
    <section class="home-list">
        {% if postsByCategory %}
            {% for categoryId, posts in postsByCategory %}
                <section class="home-category">
                    {% embed "home/postDeck.twig" %}
                        {% block deckTitle %}<a href="?page=blog&amp;category-id={{ categoryId }}">{{ categories[loop.index0].getName }}</a>{% endblock %}
                    {% endembed %}
                </section>
            {% endfor %}
        {% else %}
            Aucune catégorie n'est diponible.
        {% endif %}
    </section>
{% endblock %}